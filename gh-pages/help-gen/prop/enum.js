'use strict'

/**
 * Defines or overrides the allowable values for a property
 * @chainable
 * @method EnumProp#values
 * @param {String[]} values The allowable values for the property
 * @returns {Prop} this
 * @example Prop.enum().path('a').values(['b', 'c'])
 **/

